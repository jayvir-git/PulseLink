// Replaced with a literal at build time by `define` in vite.config.ts, so the
// ordinary build drops the demo adapter instead of shipping it as dead code.
// Build or run the demo with `npm run build:demo` / `npm run dev:demo`.
export const IS_DEMO: boolean = __DEMO__;
